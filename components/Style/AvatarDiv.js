import styled from "styled-components";
import { View } from "react-native";
export const AvatarDiv = styled(View)`
  display: flex;
  flex-direction: row;
  align-items: center;
  padding: 0 8px;
`;
